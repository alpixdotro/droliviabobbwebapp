{
  "navigationFallback": {
    "rewrite": "/index.html"
  },
  "routes": [
    {
      "route": "/data-api/*",
      "methods": [ "PUT", "POST", "PATCH", "DELETE" ],
      "allowedRoles": [ "authenticated" ]
    }
  ]
}